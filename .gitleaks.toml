[extend]
useDefault = true

[allowlist]
description = "Ignore vendored PAI docs with placeholder/example tokens (current libs/pai path and the pre-move pai/ history path)"
paths = [
  '''libs/pai/.*''',
  '''^pai/.*''',
]
